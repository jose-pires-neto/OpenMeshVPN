# OpenMeshVPN — Dependências Python (Windows)
# Instale com: pip install -r requirements.txt

# API Web (backend headless)
Flask==3.1.0
Flask-Cors==5.0.0

# Criptografia do túnel P2P (ChaCha20Poly1305)
cryptography==43.0.3

# Monitoramento de processos (Game Boost)
psutil==6.1.0

# Interface TAP-Windows (driver de rede virtual)
pywin32==308